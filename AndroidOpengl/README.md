# SDL3
Example of using SDL3 to render a triangle with opengl

Compile on windows by running build.bat from a developer cmd.

For android, test install on a device by running gradlew installDebug (better instructions in other projects).